# Runtime and test dependencies, pinned for reproducible installs.
#
# Deliberately a single file: the assignment's setup instruction is
#   pip install -r requirements.txt
# and `pytest` must work straight afterwards. Splitting runtime and dev
# dependencies is tidier and is what a production repository should do, but it
# would break that one-command setup for anyone following the brief literally.

# --- Runtime -----------------------------------------------------------------
Django==5.2.17
djangorestframework==3.18.0
# Query-parameter filtering that validates its input, so an unknown filter
# value returns 400 instead of silently matching nothing.
django-filter==26.1
# Reads settings from the environment with type casting, keeping configuration
# out of the codebase.
django-environ==0.14.0

# --- Testing -----------------------------------------------------------------
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
factory_boy==3.3.3
