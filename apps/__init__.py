"""Container package for this project's Django applications.

Keeping applications under a single package rather than scattered at the
repository root separates our domain code from project configuration and
tooling, and makes the boundary between the two obvious.
"""
