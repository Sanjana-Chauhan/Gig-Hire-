"""Domain constants for the gigs app."""

MAX_TITLE_LENGTH = 200

# Category is free text in the specification ("e.g. editing, design, writing"),
# not an enumeration -- so it is bounded rather than constrained to a fixed set.
MAX_CATEGORY_LENGTH = 50
